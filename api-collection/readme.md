API collections will not be maintained for all time. Hence for working collections, checkout only the commits that change the json file.

> [!TIP]
> Known issue on Windows when importing Bruno JSON files. Files saved in Git may use CR line endings, while Bruno expects CRLF.  
> If import fails, convert the file to CRLF format and retry.
